/**
 * shekharkhedekar.com
 * est. 8/25/14
 */

$(document).ready(function () {
    var $document = $(document),
        $window = $(window),
        $body = $('body');

    $document.on('scroll', function (e) {

    });
});